{
    "name": "asd-lead-generation",
    "private": true,
    "packageManager": "pnpm@10.0.0",
    "scripts": {
        "build": "turbo build",
        "dev": "turbo dev",
        "test": "turbo test"
    },
    "devDependencies": {
        "turbo": "^2.9.3"
    },
    "pnpm": {
        "overrides": {
            "@tanstack/react-query": "5.101.2"
        }
    }
}
